问题描述
　　编写一个程序，首先输入一个整数，例如5，然后在屏幕上显示如下的图形（5表示行数）：
　　* * * * *
　　* * * *
　　* * *
　　* *
　　*
  #include <iostream>
#include <stdlib.h>
using namespace std;

int main()
{
    int n = 0;
    cin >> n;
    if (n <= 0)
        exit(0);
    while(n)
    {
        for (int i = 0; i < n; i++)
        {
            cout << "* ";
        }
        cout << endl;
        n--;
    }

    return 0;
}

  
